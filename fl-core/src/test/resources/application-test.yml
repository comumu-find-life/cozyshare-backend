spring:
  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 100
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.MySQLDialect
    defer-datasource-initialization: true

  data:
    mongodb:
      uri: mongodb://root:sin981023@localhost:27017/testdb?authSource=admin
      database: testdb

  datasource:
    initialization-mode: never
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fl_test?serverTimezone=Asia/Seoul
    username: root
    password: sin981023@
